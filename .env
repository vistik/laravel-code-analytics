APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:somekey==
APP_DEBUG=true

# Ollama runs locally — no API key required.
# Override the host if Ollama is not on the default http://localhost:11434
# OLLAMA_HOST=http://localhost:11434
