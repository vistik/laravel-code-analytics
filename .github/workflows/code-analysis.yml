name: Code Analysis

on:
  pull_request:
    paths:
      - '**.php'
      - '.github/workflows/code-analysis.yml'

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      pull-requests: write
      pages: write
      id-token: write
    environment:
      name: github-pages

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch base branch
        run: git fetch origin ${{ github.base_ref }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: none

      - name: Install composer dependencies
        uses: ramsey/composer-install@v4

      - name: Generate HTML report
        run: |
          ./testbench code:analyze \
            --base=origin/${{ github.base_ref }} \
            --format=html \
            --title="PR #${{ github.event.pull_request.number }}: ${{ github.event.pull_request.title }}" \
            --no-ansi \
            . report.html

      - name: Capture metrics summary
        id: metrics
        run: |
          SUMMARY=$(./testbench code:analyze \
            --base=origin/${{ github.base_ref }} \
            --format=metrics \
            --no-ansi \
            . 2>&1 || true)
          echo "summary<<EOF" >> $GITHUB_OUTPUT
          echo "$SUMMARY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Collect existing reports
        run: |
          mkdir -p _site
          git fetch origin reports-cache --depth=1 2>/dev/null \
            && git archive origin/reports-cache | tar -x -C _site/ 2>/dev/null \
            || true

      - name: Add this PR's report
        run: cp report.html _site/pr-${{ github.event.pull_request.number }}.html

      - name: Save reports to cache branch
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd _site
          git init
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b reports-cache
          git add .
          git commit -m "Add report for PR #${{ github.event.pull_request.number }}"
          git remote add origin https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}
          git push origin reports-cache --force
          cd ..

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site/

      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4

      - name: Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            const marker = '<!-- code-analysis-report -->';
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const prNumber = context.issue.number;
            const reportUrl = `${{ steps.deploy.outputs.page_url }}pr-${prNumber}.html`;
            const runUrl = `https://github.com/${owner}/${repo}/actions/runs/${context.runId}`;
            const metrics = `${{ steps.metrics.outputs.summary }}`;

            const body = [
              marker,
              '## Code Analysis Report',
              '',
              metrics ? `\`\`\`\n${metrics}\n\`\`\`` : '',
              '',
              `[View HTML Report](${reportUrl}) · [View Run](${runUrl})`,
              '',
              '_Automatically updated on each push._',
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner, repo, issue_number: prNumber,
            });
            const existing = comments.find(c => c.body && c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner, repo, comment_id: existing.id, body,
              });
            } else {
              await github.rest.issues.createComment({
                owner, repo, issue_number: prNumber, body,
              });
            }

      - name: Update PR description
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const prNumber = context.issue.number;
            const reportUrl = `${{ steps.deploy.outputs.page_url }}pr-${prNumber}.html`;
            const start = '<!-- code-analysis-start -->';
            const end = '<!-- code-analysis-end -->';
            const badge = `${start}\n📊 [Code Analysis Report](${reportUrl})\n${end}`;

            const { data: pr } = await github.rest.pulls.get({
              owner, repo, pull_number: prNumber,
            });
            let description = pr.body || '';
            if (description.includes(start)) {
              description = description.replace(
                new RegExp(`${start}[\\s\\S]*?${end}`),
                badge
              );
            } else {
              description = badge + (description ? '\n\n' + description : '');
            }
            await github.rest.pulls.update({
              owner, repo, pull_number: prNumber, body: description,
            });
